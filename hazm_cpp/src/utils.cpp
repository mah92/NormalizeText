#include "hazm/utils.hpp"

// All functions are inline in the header.
// This file exists to satisfy the build system (CMakeLists.txt references it).
namespace hazm {
    // Empty — all implementations are in utils.hpp as inline functions
}